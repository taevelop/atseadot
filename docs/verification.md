# 게임 오류 수정 및 검증

검증일: 2026-09-10. Cloudflare Pages 로컬 런타임(`wrangler pages dev`)과 Chrome에서 확인했습니다.

| 문제 | 수정된 동작 | 검증 |
| --- | --- | --- |
| 낚시 메뉴를 클릭해도 잠수부로 시작 | 그려진 메뉴 항목의 영역으로 선택을 판정 | 5개 메뉴 회귀 테스트, 브라우저에서 낚시 메뉴 클릭 |
| 터치로 포획·역할 전환을 할 수 없음 | 이동·가속·포획·역할 전환·도감·도움말·뒤로 버튼 제공 | 브라우저 버튼으로 그물 포획 → 결과 닫기 → 배 전환 → 줄 던지기 |
| 입질 알림을 넘기느라 챔질이 늦음 | 입질 중에는 포획 버튼과 Space가 즉시 챔질 | 알림이 있는 상태에서 버튼을 한 번 눌러 챔질 성공 |
| 낚시 기록에 수면 수심이 저장됨 | 챔질 순간의 수심을 결과 카드와 첫 포획 기록에 사용 | 750m 챔질 → 화면 크기 변경 → 회수 완료 후 카드·기록 모두 750m |
| 역할을 바꾸면 걸린 물고기가 계속 정지 | 낚싯줄 초기화 시 대상 물고기의 정지를 해제 | 입질·회수 중 역할 전환 및 플레이어 초기화 테스트, 브라우저 확인 |
| 창 크기 변경 시 바다와 수심이 바뀜 | 기존 생물·상자를 유지하고 좌표를 새 크기에 맞춤 | 450m에서 데스크톱·세로·가로 전환 후 수심, 생물 객체, 열린 상자 유지 |
| 도움말 아래 항목이 잘림 | 실제 글자 폭과 화면 높이에 따라 페이지를 구성 | 한국어·영어 16개 항목 전체 검사, 터치로 마지막 페이지까지 이동 |
| 모바일 영문 도감 제목·탭·상세 글자가 겹침 | 제목 줄 분리, 탭 길이 조정, 상세 내용 줄바꿈과 스크롤 | 해마 상세 카드 및 영문 도감 스크린샷, 모든 생물의 메타데이터 폭 검사 |
| 손상된 저장 데이터가 첫 화면을 중단 | 필드별 유효성 검사로 정상 기록을 보존하면서 복구 | 잘못된 JSON·배열·null·필드 자료형 검사, 실제 브라우저에서 `caught: null` 복구 |
| 0m 첫 포획 기록이 덮어써짐 | 값의 존재 여부로 최초 기록을 판정 | 0m 기록을 가진 생물 재포획 후 원래 값 유지 |
| 페이지를 닫기 직전 기록이 유실될 수 있음 | `pagehide`와 숨김 전환 시 대기 중인 저장을 즉시 수행 | 종료·숨김 저장 및 저장소 사용 불가 상태 회귀 테스트 |
| 일시정지 중 충돌·포획 처리가 실행될 수 있음 | 일시정지 시 물리 업데이트와 포획 액션 중단 | 이동·포획·진행 기록 불변 검사, 모바일 재개 버튼 확인 |
| 터치 입력이 해제되지 않거나 안내 글자가 넘침 | 포인터 취소·캡처 해제·포커스 상실 시 입력 해제, 안내 폭 제한 | 브라우저에서 이동 버튼 누름·이동·포커스 상실 후 입력 해제 |

## 자동 검증

```sh
npm test
npm run build
```

`tests/game.test.cjs`, `tests/virtual-controls.test.cjs`, `tests/typography.test.cjs`의 회귀 테스트 총 25개가 통과했습니다. `build`는 JavaScript 구문 검사와 이 테스트를 모두 실행합니다. 테스트는 실제 게임 스크립트를 Node VM에서 실행하며, 시간·난수·저장소·Canvas·DOM 환경을 대체합니다.

## 브라우저 검증

- 데스크톱 1258×622, 모바일 세로 390×844, 가로 844×390에서 확인했습니다. 가로 화면은 Chrome의 터치 에뮬레이션을 활성화해 화면 너비에 따른 배치까지 확인했습니다.
- 포획 수심과 화면 전환 검증에는 게임 상태를 준비한 뒤 실제 버튼을 누르는 방식을 사용했습니다. 자연 발생하는 입질을 기다리는 장시간 플레이 테스트는 포함하지 않았습니다.
- 첫 화면, 도움말, 도감 목록·상세, 포획 결과, 모바일 가로·세로 화면을 스크린샷으로 확인했습니다.
- 주요 조작 및 저장 복구 후 브라우저 JavaScript 오류는 0건이었습니다.
- Pages 응답 경로와 앱 아이콘은 기존 정적 배포 구조를 사용합니다. 이 검증은 로컬 실행을 대상으로 합니다.

## 모바일 가상 컨트롤러

- 왼쪽 원형 다이얼: 중앙 정지 영역, 8방향 이동, 손가락을 떼지 않는 방향 전환, 바깥쪽 가속, 누른 방향과 가속 상태 표시.
- 오른쪽 버튼: 82px 액션 버튼과 56px 가속 버튼. 액션은 포획·던지기·챔질·닫기·재개 상태에 맞춰 표시되며, 손가락을 뗄 때 중복 실행되지 않습니다.
- 다이얼과 액션·가속 버튼은 독립적인 포인터를 처리합니다. 다른 손가락을 떼어도 다이얼 입력을 취소하지 않습니다.
- 화면 회전, 앱 전환, 터치 취소 시 이동 입력과 버튼 캡처를 해제합니다. 메뉴·역할·일시정지 상태 전환 시 이전 이동 입력을 정리합니다.
- 한국어·영어, 320×640·390×844·568×320·844×390에서 화면 넘침과 게임 영역 가림이 없고, 버튼 터치 영역이 44px 이상임을 확인했습니다.
- Chrome의 실제 다중 터치 이벤트로 대각선 이동 중 포획, 드래그 방향 전환, 다이얼 밖으로 이동한 상태의 캡처, 가속 버튼 동시 누름, 터치 취소를 검증했습니다.
- 포획 결과가 손가락을 뗀 뒤에도 유지되고, 다이얼로 도움말 페이지를 넘길 수 있음을 확인했습니다. 450m에서 화면 회전 후 수심과 생물 상태도 유지됐습니다.
- 가상 컨트롤러 검증 후 브라우저 JavaScript 오류는 0건이었습니다. 실제 iOS·Android 기기 검증은 포함하지 않습니다.

## 가상 조작부 표시 범위

- 화면 너비 1024px 이하에서만 표시하고, 1025px 이상에서는 숨깁니다. 입력 장치 종류와 터치 지원 여부는 표시 조건에 사용하지 않습니다.
- Chrome에서 390·768·1024px 표시, 1025·1280px 숨김을 확인했습니다. 터치가 지원되는 데스크톱 조건에서도 숨겨지고 게임 캔버스가 전체 높이를 사용합니다.
- 1024px에서 다이얼을 누른 채 1025px로 전환하면 입력이 해제되고, 숨겨진 버튼에서 포커스가 빠져 키보드 조작이 이어지는 것을 확인했습니다.

## 모바일 한글 픽셀 글자

검증일: 2026-09-10. 사용자가 제공한 iPhone 16 Pro 화면의 한글 획 손상과 가독성 문제를 기준으로 개선했습니다.

### 적용 내용

- Galmuri11 웹폰트를 함께 배포하고 `@font-face`와 preload로 로딩합니다. 시스템 글꼴을 11px로 그려 알파 값 96 이하의 획을 삭제하던 처리를 제거했습니다.
- Galmuri11은 12px가 기준 크기입니다. 3배 크기로 먼저 렌더링하고 최근접 샘플링으로 원래 픽셀 격자에 맞춥니다. Windows Chrome에서 직접 12px로 그리면 반투명 가장자리가 남았지만 3배 샘플링에서는 검사한 글자의 알파가 0 또는 255로 유지됐습니다.
- 첫 프레임은 폰트 로딩 후 시작합니다. 로딩이 2초보다 오래 걸리거나 실패하면 기본 글꼴로 시작하며, 늦게 도착한 폰트는 글자 캐시와 현재 대사 줄바꿈을 갱신합니다. 바다와 진행 상태는 다시 생성하지 않습니다.
- 한글의 기본 그림자를 없애고 보조 글자를 밝게 조정했습니다. 줄 간격은 논리 화면에서 14px, 메뉴 행은 16px입니다. 긴 기록은 수심·수집 수 단위로 나눠 어두운 안내 패널에 표시합니다.
- 캔버스 메모리 크기는 CSS 크기와 기기 픽셀 밀도에 맞춥니다(밀도 상한 4). 게임 픽셀은 물리 픽셀의 정수 배율로 그리며 오른쪽·아래쪽의 한 칸 미만 여분만 잘라냅니다. 포인터 좌표에도 같은 배율을 적용합니다.
- 좁은 화면의 일시정지 표시는 상단 기록 아래에 배치합니다. 제목 화면 높이가 부족하면 큰 로고를 생략해 메뉴와 기록 공간을 확보합니다.

### 검증 결과

| 브라우저 뷰포트(CSS px) | 기기 픽셀 비율 | 결과 |
| --- | --- | --- |
| 402×874 | 3 | 메뉴·기록·전체 도감·도움말 폭 검증 통과 |
| 360×640 | 3 | 메뉴·기록·전체 도감·도움말 폭 검증 통과 |
| 874×402 | 3 | 가로 화면의 같은 항목 검증 통과 |
| 390×844 | 2.625 | 소수 배율에서 같은 항목 검증 통과 |
| 1280×800 | 1 | 데스크톱의 같은 항목 검증 통과 |

- 모든 조건에서 한국어·영어로 긴 기록과 전체 생물 메타데이터, 도움말 16개 항목의 접근 가능 여부와 가로 넘침을 확인했습니다. 검사한 한글 메뉴·수심계·기록 문자열 11개의 비트맵에서 반투명 픽셀은 0개였습니다.
- 실제 브라우저 포인터로 낚시 메뉴를 눌러 배로 시작하는 것을 확인했습니다. 일시정지한 911m 상태에서 가로·세로 전환 후 수심과 생물 객체가 유지됐습니다. 이 검증에는 준비한 게임 상태를 사용했습니다.
- 제목 화면, 수심계, 도움말, 해마 상세 카드, 가로 화면을 시각적으로 확인했습니다. 브라우저 JavaScript 오류는 0건입니다.
- 새 자동 테스트 6개는 폰트 정상 로딩·실패·지연, Retina·소수 배율, 배율 변경 후 상태와 클릭 좌표, 긴 기록과 메뉴 배치를 다룹니다. 전체 빌드와 25개 회귀 테스트가 통과했습니다.
- 자동 테스트는 Canvas와 글자 폭을 대체하므로 글자 모양은 위 Chrome 검증으로 보완했습니다. iPhone 16 Pro의 실제 Safari·홈 화면 앱에서 수정본을 확인한 결과는 아직 포함하지 않습니다.

미리보기는 402×874, 픽셀 비율 3의 Chrome에서 추출한 게임 캔버스이며 실제 iPhone 캡처가 아닙니다. 터치 조작부는 포함하지 않습니다.

- [메뉴와 기록](screenshots/mobile-typography-title.png)
- [수심계와 일시정지](screenshots/mobile-typography-hud.png)

### 폰트 출처

- [Galmuri 공식 저장소](https://github.com/quiple/galmuri), 버전 2.40.3, 고정 커밋 `71e1cacf1437a11220307120e63e30bc275312d4`.
- [포함한 원본 WOFF2](https://raw.githubusercontent.com/quiple/galmuri/71e1cacf1437a11220307120e63e30bc275312d4/dist/Galmuri11.woff2): 505,400바이트. SHA-256: `8bad9322b3340bfb5cb26cb00f4752bf4c8e7d1b526bb07205f4af458eebed31`.
- 원본 폰트 파일은 수정하지 않았으며 [OFL 라이선스](../site/assets/fonts/OFL-Galmuri.txt)를 함께 배포합니다.
- 구현 참고: [폰트 로딩 API](https://developer.mozilla.org/en-US/docs/Web/API/CSS_Font_Loading_API), [Canvas 픽셀 정수 확대](https://developer.mozilla.org/en-US/docs/Games/Techniques/Crisp_pixel_art_look).
